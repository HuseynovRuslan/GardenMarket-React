# Loaded only by `vite dev`. Points the storefront at a locally-running
# GardenMarket-API (see that repo — default PORT 3100). Production builds ignore
# this file and use the Dockerfile's VITE_API_BASE build arg instead.
VITE_API_BASE=http://localhost:3100
